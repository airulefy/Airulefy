{
  "name": "Airulefy Dev",
  "build": { "dockerfile": "Dockerfile" },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "postCreateCommand": "poetry install && poetry run pytest",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}"
}
